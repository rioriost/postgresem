# Automated v1.0.1 release delta review

Date: 2026-09-12. This is an automated source/delta review, not independent
third-party review, image review, or field operation evidence.

Reviewed commit: `d56b8fffffde89961f71562a42d2255fb913feb6`.
Stable manifest SHA-256:
`2f43961ee31df5bb12f89aa974b6d625b8821756dfbc07ac11e6554ee8e53015`.

## Scope and findings

Compared with v1.0.0, application runtime source, external dependencies,
schemas, migrations, permissions and public contract fields are unchanged.
Cargo package versions and the manifest release label move to 1.0.1. The
operator integration check expects the new label. The stable inventory tracks
those changes and the new packaging inputs. The prior source remediation
record remains in `docs/security-reviews/2026-09-05-381fe57.md` unchanged.

The macOS release workflow imports the executables already built with Xcode
27 RC and accepted by Apple notarization. It verifies pinned archive and
executable SHA-256, regular-file type, Developer ID signature/team, exact
release/architecture and SDK 27 before use. It extracts only the executable,
then packages schemas/contracts/docs from the actual tagged source. Imported
metadata cannot replace the tagged release evidence. The pinned input record
and import script are part of the frozen artifact inventory.

Reviewed release-code paths introduce no credential logging, shell command
interpolation, arbitrary archive extraction, or optional verification bypass.
A wrong input digest or target fails closed. The original native macOS smoke,
Linux runtime/image smoke, checksum OIDC signing, container SBOM/provenance,
and container image signing remain in the release workflow.

## Verified evidence and limitations

- The existing 156 workspace tests passed for the version-only runtime change.
- All seven CI jobs at source `f5ab30c118abf6f6371b5ac6674d4b06d15c27c4`
  passed: https://github.com/rioriost/postgresem/actions/runs/34678913572
- Both signed input archives were downloaded through the new importer and
  passed the archive/binary digest, signature/team and SDK checks locally.
- arm64 runtime version/contract smoke passed. The final release workflow
  must still execute the actual imported amd64 binary on an Intel runner.
- New policy/gate changes are separate from this reviewed baseline and must
  pass rejection tests and immutable-identity validation before release.

No unresolved P0/P1 finding was identified in this narrow release delta.
This does not assert zero real-world defects or a completed independent
security assessment. No reviewed image digest or field pilots exist.
