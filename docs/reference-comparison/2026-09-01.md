# M7 reference comparison — 2026-09-01

This comparison is a scope and architecture comparison, not a general product
ranking. OSS capabilities are separated from hosted or commercial features.
The common target is PostgreSQL, but postgresem remains the only evaluated
engine whose supported execution contract requires PostgreSQL to enforce the
final GRANT/RLS decision.

## Locked references

| Project | Evaluated release/ref | Scope |
|---|---|---|
| Wren AI | `wrenai 0.13.3`, `wren-core-py 0.7.6`, commit [`992f1da`](https://github.com/Canner/WrenAI/tree/992f1da21b94293c72881484135cbab88fb04b31) | Current OSS CLI, SDK, core, local MCP, MDL, and import behavior |
| Cube | [`v1.7.31`](https://github.com/cube-js/cube/releases/tag/v1.7.31) | Cube Core OSS; Cloud-only features are marked separately |
| Malloy | [`v0.0.433`](https://github.com/malloydata/malloy/releases/tag/v0.0.433) plus Publisher documentation as of this date | Malloy compiler and OSS Publisher |
| MetricFlow | [`v0.212.0`](https://github.com/dbt-labs/metricflow/releases/tag/v0.212.0) | Apache-2.0 compiler and converters; dbt hosted APIs are separate |
| Apache Ossie | core spec `0.1.1`, commit [`faf5810`](https://github.com/apache/ossie/blob/faf581054dcf7964d5fe0ceae7d6f415c8ce32a5/core-spec/spec.md) | Stable interchange contract accepted by the M7 importer |
| postgresem | [`v0.4.0`](https://github.com/rioriost/postgresem/releases/tag/v0.4.0) | Query, mutation, audit, PostgreSQL 16–18, Linux amd64/arm64 |

The Ossie `main` branch currently identifies itself as `0.2.0.dev0`. It is
useful design evidence but is not an accepted import contract.

## Common PostgreSQL tasks

The executable postgresem side reuses the three datasets and 30 accepted or
fail-closed questions in
[`fixtures/evals/m0-evals.json`](../../fixtures/evals/m0-evals.json). M7 adds:

| Task | Fixture/evidence | What is compared |
|---|---|---|
| Portable model conversion | [`osi-commerce.yaml`](../../fixtures/interoperability/osi-commerce.yaml) plus role-bound [catalog evidence](../../fixtures/interoperability/osi-commerce-catalog.json) | Model, direct fields, key-backed relationship, count/sum metrics |
| Structured aggregate query | [`orders-revenue.json`](../../examples/commerce/orders-revenue.json) | Closed semantic request to deterministic SQL |
| Unsafe expression rejection | OSI importer unit cases | Computed/raw SQL and multi-dialect expressions are rejected |
| Catalog drift | `catalog scan` + `catalog diff` unit and CLI cases | Type, key, GRANT, RLS, and policy changes |
| Tenant isolation | existing `rls-multitenant` eval and integration fixtures | Database-authoritative principal propagation |
| Governed write | existing LSM insert/upsert integration fixtures | Bounded writes, RLS `WITH CHECK`, idempotency, atomic audit |

Reference tools express equivalent read tasks through their own model/query
languages. Mutation and PostgreSQL-authoritative security are marked absent
rather than emulated through materialization, export, or service-side filters.

## Capability and gap matrix

| Capability | Wren AI OSS | Cube Core OSS | Malloy/Publisher OSS | MetricFlow OSS | postgresem 0.4/M7 direction |
|---|---|---|---|---|---|
| Model source of truth | MDL YAML and derived manifest | YAML/JS files | `.malloy` packages | dbt semantic manifest/YAML | Immutable revision in target PostgreSQL |
| PostgreSQL catalog evidence | Connector/schema discovery; not runtime authority | Schema generation | Connection schema | dbt artifacts | COMMENT/type/PK/FK/CHECK/GRANT/RLS scan plus fingerprinted drift gate |
| Structured query contract | Cube query exists; primary surfaces also accept SQL | JSON REST/GraphQL plus PostgreSQL-wire SQL | Typed query builder plus Malloy language | CLI/Python metric request | Strict LSQ; no raw SQL public query |
| Arbitrary SQL surface | Yes, including local MCP `run_sql` | Yes through SQL API and model SQL | Malloy expressions compile to SQL | Compiler input is metric selection, not execution SQL | Rejected |
| Basic aggregate/time semantics | Strong cube measures and grains | Strong measures and calendar cubes | Rich language and aggregate locality | Rich metric/time-spine system | Safe v1 aggregate/grain subset |
| Fan-out and multi-fact | Relationship-aware; not the main differentiator | Multiplied-measure and multi-fact planning | Explicit aggregate locality | Entity/dataflow planning | Unsafe ambiguous paths rejected; explicit extension deferred to 0.6 |
| Governed caller writes | None | None; pre-aggregation is operator materialization | None; persistence is operator materialization | None; exports are compiled/scheduled artifacts | Separate typed LSM insert/upsert, writer role, RLS, idempotency, audit |
| Final authorization authority | Planner RLAC/CLAC from session properties | Generated filters from security context | Publisher authorization gates and caller givens | No execution authority | PostgreSQL GRANT/RLS; unsafe role graphs rejected |
| Immutable publication | No; generated manifest can be stale | Git/file deployment | Reload may serve previous compiled package as stale | dbt artifact/Git | One published revision per project with canonical hash |
| Lineage/audit | OSS has no equivalent query audit | Product metadata; richer hosted operations | Compiler metadata and Publisher status/metrics | Dataflow plan; hosted lineage is separate | Per-operation revision/compiler/source lineage and atomic audit |
| MCP/API | Local stdio/HTTP MCP; HTTP has no built-in auth | REST/GraphQL/SQL OSS; MCP is hosted | REST and MCP Publisher | Compiler/CLI; dbt MCP is separate | Capability-gated stdio MCP; authenticated HTTP deferred |
| Cache/pre-aggregation | No primary PostgreSQL-native cache | Cube Store/pre-aggregations | Same-connection persistence/pre-aggregation | Hosted caching/exports | Deferred; same-PostgreSQL materialization is preferred |
| Interchange | Ossie and dbt import | dbt/LookML and hosted sync options | Limited | Bidirectional Ossie converters | One-way Ossie 0.1.1 candidate import, verified against PostgreSQL |
| PostgreSQL release evidence | Connector tests pin PostgreSQL 16 | Supported driver, broad engine focus | PostgreSQL dialect/connection | PostgreSQL renderer | PostgreSQL 16, 17, 18 integration/recovery gates |

## Decisions

**Adopt now**

- One-way Apache Ossie import as a reviewable candidate, with PostgreSQL catalog
  evidence resolving physical type, nullability, PK, FK, and visibility.
- Fingerprinted catalog drift reporting with GRANT/RLS/constraint changes
  treated as breaking.
- Structured conversion issues and warnings; no silent fallback to generic
  types, prose metrics, or a full external manifest.

**Defer**

- Explicit fan-out-safe aggregation anchors, modeled after Malloy aggregate
  locality and Cube multiplied-measure planning, to M8.
- Time spines, cumulative/derived metrics, and custom calendar tables, using
  MetricFlow and Cube as semantic references, to M8.
- Same-PostgreSQL pre-aggregation, closer to Publisher persistence than a
  mandatory second datastore, to M10.
- PostgreSQL wire serving until an ADR can preserve the no-raw-SQL boundary.

**Reject intentionally**

- Caller-asserted identity or session attributes as the final authorization
  source.
- Unauthenticated remote MCP.
- External semantic files as a live runtime authority.
- Multi-dialect execution, arbitrary imported SQL expressions, and silent
  lossy conversion.
- Mandatory cache, vector, or secondary metadata infrastructure.

## Differentiated position

postgresem does not attempt to replace the GenBI workflow, broad APIs, modeling
language expressiveness, or cache systems of the references. It is the narrow
gateway for teams that want meaning, revision history, query/write contracts,
identity enforcement, and audit evidence to stay inside the same PostgreSQL
operational boundary as the data.

That position is strongest where the references are weakest or intentionally
out of scope: immutable in-database publication, database-enforced RLS
principal propagation, a no-raw-SQL agent contract, governed insert/upsert, and
catalog drift that treats authorization metadata as first-class evidence.

## Reproduction

Run the repository-owned subset without a database:

```sh
make test-reference-comparison
```

For runtime enforcement, run the PostgreSQL 16–18 and Linux architecture jobs
documented in [compatibility](../compatibility.md). External reference runtimes
are version-pinned above but are not installed in postgresem CI; their entries
are based on the linked release source and official documentation, so
cloud-only services are not accidentally counted as OSS behavior.
